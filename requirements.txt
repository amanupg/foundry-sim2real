# Foundry: image-to-physics-ready-asset pipeline
# Pinned to Python 3.11. CPU-only mesh/physics. Hosted inference via Replicate + OpenRouter.
#
# NOTE: coacd, pybullet, rembg, onnxruntime have prebuilt wheels for CPython 3.11 on macos arm64.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
python-multipart==0.0.20
pydantic==2.10.4

# Mesh / geometry / image ops (CPU)
numpy==2.3.5
trimesh==4.5.3
pillow==12.1.0
rembg==2.0.76
onnxruntime==1.21.0
# coacd approximate convex decomposition (falls back to convex hull on failure)
coacd==1.0.11

# Physics simulation (CPU, headless)
pybullet==3.2.7

# Hosted inference clients
replicate==1.0.7
httpx==0.28.1
